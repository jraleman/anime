// TODO: We can rename `username` -> `defaultUsername`
// and add a text input so user can enter their username if they want
export const username = 'jraleman';
export const animeListEndpoint = `/users/${username}/animelist`;
